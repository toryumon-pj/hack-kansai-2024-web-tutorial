import { PaletteOptions } from '@mui/material';
import { constThemeColor } from '../const/const';

/**
 * MUITheme/palette
 */
export const paletteTheme: PaletteOptions = {
  primary: {
    main: constThemeColor.primary.main,
    light: constThemeColor.primary.light,
  },
};
